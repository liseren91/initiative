{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "HUSKY=0 pnpm run build"
  },
  "deploy": {
    "startCommand": "pnpm exec vite preview --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
